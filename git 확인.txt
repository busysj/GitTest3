git init
git add .
git commit -m " "
git log
 > 폴더에 커밋을 남김

git remote add origin " "
git push -u origin ' '
 > 원격 저장소에 저장

git branch ' '
git checkout ' '
git branch

git merge ' '
 > 항상 master branch에서 merge 

merge 충돌 시
1. 충돌된 파일 확인
2. 원하는 코드만 남겨둠
3. git add .
4. git commit -m " "

